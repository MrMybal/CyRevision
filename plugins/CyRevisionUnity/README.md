# CyRevision Unity companion

Open **Tools > CyRevision > Connection** inside Unity to test the private link, notify CyRevision, or open the desktop application for the current project. Connection data is stored under `Library/CyRevision` and is not intended for Git.

This first version supports project detection, installation, connection, and project-change notifications. It does not add Unity asset previews or semantic asset diffs yet.
